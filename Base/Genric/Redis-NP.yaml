apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: redis-np
spec:
  podSelector:
    matchLabels:
      app: redis
  policyTypes:
  - Ingress
  ingress:
  - from:
    - podSelector:
        matchLabels:
          app: Cartservice
    ports:
    - protocol: TCP
      port: 6379
