apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: currencyservice-policy
spec:
  podSelector:
    matchLabels:
      app: Currencyservice
  policyTypes:
    - Ingress    
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: Frontendservice
        - podSelector:
            matchLabels:
              app: Checkoutservice
      ports:
        - protocol: TCP
          port: 7000
  