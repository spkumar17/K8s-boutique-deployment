apiVersion: v1
kind: Service
metadata:
  name: checkoutservice
  labels:
    app: checkoutservice
spec:
  type: ClusterIP
  selector:
    app: checkoutservice
  ports:
  - name: grpc  
    port: 5050       # Service port (used inside cluster)
    targetPort: 5050 # Pod's containerPort


