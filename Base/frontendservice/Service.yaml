apiVersion: v1
kind: Service
metadata:
  name: frontendservice
  labels:
    app: frontendservice
spec:
  type: ClusterIP
  selector:
    app: frontendservice
  ports:
  - name: http
    port: 8080       # Service port (used inside cluster)
    targetPort: 8080 # Pod's containerPort
