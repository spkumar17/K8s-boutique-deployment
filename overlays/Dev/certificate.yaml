apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: certificate-dev
  namespace: development
spec:
  secretName: ingress-dev-tls-secret
  issuerRef:
    name: letsencrypt-dev   # 🔑 Must match the Issuer in same namespace
    kind: Issuer
  dnsNames:
  - app.nextgendev.site  
