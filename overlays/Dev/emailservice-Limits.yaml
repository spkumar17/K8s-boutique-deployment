apiVersion: apps/v1
kind: Deployment
metadata:
  name: emailservice
  namespace: development
  labels:
    app: dev-emailservice
spec:
  template:
    spec:
      containers:
      - name: emailservice
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
