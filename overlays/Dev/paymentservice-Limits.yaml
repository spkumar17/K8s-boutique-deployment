apiVersion: apps/v1
kind: Deployment
metadata:
  name: Paymentservice
  namespace: development
  labels:
    app: dev-Paymentservice
spec:
  template:
    spec:
      containers:
      - name: Paymentservice
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
