apiVersion: apps/v1
kind: Deployment
metadata:
  name: dev-paymentservice
  namespace: development
  labels:
    app: paymentservice
spec:
  template:
    spec:
      containers:
      - name: paymentservice
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
