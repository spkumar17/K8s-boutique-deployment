apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: redis
  namespace: development
spec:
  volumeClaimTemplates:
    - metadata:
        name: redis-storage
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 1Gi
