apiVersion: apps/v1
kind: Deployment
metadata:
  name: Currencyservice
  namespace: development
  labels:
    app: dev-Currencyservice
spec:
  template:
    spec:
      containers:
      - name: Currencyservice
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
